#include "XREX.hpp"

#include "Settings.hpp"

namespace XREX
{

	RenderingSettings::RenderingSettings()
		: fullScreen(false), left(200), top(200), width(800), height(600), colorBits(32), depthBits(24), stencilBits(8), sampleCount(1)
	{

	}

}
